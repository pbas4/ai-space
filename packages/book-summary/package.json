{
  "name": "@pbas/book-summary",
  "version": "0.3.0",
  "private": true,
  "description": "Skill: summarize a book or document (EPUB/PDF/DOCX/HTML/TXT/URL) into templated Markdown + PDF via model-split subagents, and file it to Google Drive and Obsidian.",
  "scripts": {
    "test": "bash tests/run.sh",
    "validate": "bash skills/book-summary/scripts/check_deps.sh || true; bash tests/run.sh"
  }
}
