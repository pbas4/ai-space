{
  "name": "@pbas/book-summary",
  "version": "0.4.0",
  "private": true,
  "description": "Skill: summarize EPUB/PDF books into templated Markdown + PDF and file them to Google Drive and Obsidian.",
  "scripts": {
    "test": "bash tests/run.sh",
    "validate": "bash skills/book-summary/scripts/check_deps.sh || true; bash tests/run.sh"
  }
}
