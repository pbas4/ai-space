{
  "name": "repo-graph",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": { "repo-graph": "dist/src/cli/main.js" },
  "files": [
    ".codex-plugin",
    "dist/src",
    "assets",
    "skills",
    "DEPENDENCIES.md",
    "THIRD_PARTY_LICENSES/TypeScript.txt"
  ],
  "engines": { "node": ">=22.13.0" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test/*.test.js",
    "check:offline": "node scripts/check-production-imports.mjs",
    "pack:offline": "npm run build && node scripts/build-offline-artifact.mjs"
  },
  "dependencies": { "typescript": "6.0.3" },
  "devDependencies": { "@types/node": "22.20.1" },
  "bundleDependencies": ["typescript"]
}
