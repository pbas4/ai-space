{
  "type": "object",
  "required": ["findings", "verification", "completion"],
  "properties": {
    "findings": { "type": "array", "items": {} },
    "verification": { "type": "object" },
    "completion": { "enum": ["pass", "blocked", "pass-with-findings"] }
  }
}
